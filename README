Prerequisites:

Have python installed

1. Create venv

```bash
python -m venv venv
```

2. Activate venv

```bash
venv/Scripts/activate
```

3. Installed required libraries

```bash
pip install -r requirements.txt
```

4. Insert the files to convert into folder and configure the path in script

5. Run script

```bash
python script.py
```
